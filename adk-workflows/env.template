# Google ADK Configuration
GOOGLE_GENAI_USE_VERTEXAI=FALSE
GOOGLE_API_KEY=your_google_api_key_here

# CentOS Package Updater Parameters
# These correspond to the parameters from the original Goose recipe

# Required Parameters
PACKAGE=package_name_here
VERSION=version_to_update_to
JIRA_ISSUES=comma_separated_jira_issues

# Optional Parameters with Defaults
GIT_URL=https://gitlab.com/redhat/centos-stream/rpms
DIST_GIT_BRANCH=c10s
GIT_USER=RHEL Packaging Agent
GIT_EMAIL=rhel-packaging-agent@redhat.com
GITLAB_USER=vrothberg

# GitLab CLI Configuration (if needed)
# GITLAB_TOKEN=your_gitlab_token_here

# Usage Instructions:
# 1. Copy this file to .env
# 2. Fill in the required parameters (PACKAGE, VERSION, JIRA_ISSUES, GOOGLE_API_KEY)
# 3. Modify optional parameters if needed
# 4. Run: make adk-package-updater or make adk-package-updater-web
