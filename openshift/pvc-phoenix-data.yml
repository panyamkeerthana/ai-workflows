apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: phoenix-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: gp3-csi
  volumeMode: Filesystem
